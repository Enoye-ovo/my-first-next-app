import React from 'react';

const page = (props) => {
    console.log(111);

    return (
        <div>
            <span style={{ cursor: 'pointer' }}>我测试一下git提交</span>
        </div>
    );
};

export default page;
