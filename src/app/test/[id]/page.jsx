import React from 'react';

const page = (props) => {
    console.log(111);

    return (
        <div>
            <span style={{ cursor: 'pointer' }}>我是动态路由哈哈哈</span>
        </div>
    );
};

export default page;
